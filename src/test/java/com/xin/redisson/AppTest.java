package com.xin.redisson;

import static org.junit.Assert.assertTrue;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.redisson.api.RedissonClient;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.AnnotationConfigApplicationContext;

/**
 * Unit test for simple App.
 */

public class AppTest 
{

    @Test
    public void shouldAnswerWithTrue()
    {

//        AnnotationConfigApplicationContext context = new AnnotationConfigApplicationContext(RedissonAutoConfiguration.class);
//        RedissonClient client = context.getBean(RedissonClient.class);
//        String xin = client.getBucket("xin").get().toString();
//        System.out.println(xin);
    }
}
